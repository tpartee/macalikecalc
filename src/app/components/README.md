# Components

## Button

A simple re-usable button component which supports an onClick callback and a class name attribute for styling, also takes children for content to display

Uses ButtonProps interface for object-based parameters
